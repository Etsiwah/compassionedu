# CompassionEdu Frontend - Production Environment
# Set these in Vercel Project Settings > Environment Variables.

# Backend API URL, including /api and no trailing slash.
REACT_APP_API_URL=https://YOUR_BACKEND.onrender.com/api

# Optional. Keep false in production unless you intentionally want demo credentials visible.
REACT_APP_SHOW_DEV_ACCOUNTS=false
